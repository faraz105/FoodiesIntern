import React from 'react';
import Model from "../../components/Model/Model";
const Success = ({ isOpen, closeModal }) => {
  return (
   <Model isOpen={isOpen} closeModal={closeModal}>
<h1>headigftftyf</h1>
   </Model>
  )
}

export default Success;